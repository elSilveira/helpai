# HelpAI — core dependencies (cross-platform)
openai>=1.30.0
faster-whisper>=1.2.0
SoundCard>=0.4.3
numpy>=1.24.0
mss>=9.0.0
Pillow>=10.0.0
pypdf>=4.2.0

# Windows-only
keyboard>=0.13.5; sys_platform == "win32"
pywin32>=306; sys_platform == "win32"
pystray>=0.19.5; sys_platform == "win32"

# CUDA GPU acceleration (optional, Windows/Linux)
nvidia-cublas-cu12>=12.4.0; sys_platform != "darwin"
nvidia-cudnn-cu12>=9.0.0; sys_platform != "darwin"
